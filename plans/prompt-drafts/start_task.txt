You have a task to accomplish in an Unreal Engine project using Olive AI Studio's MCP tools.

Task: {{task_description}}
Task type hint: {{task_type}}

Before building anything, orient yourself:

1. Search the project for related assets: project.search with keywords from the task. If the asset already exists, modify it rather than creating a duplicate.

2. If creating something new, check for reference patterns: blueprint.list_templates with a query matching your task domain. Library templates are extracted from real projects and show proven patterns. Factory templates are hand-crafted starting points.

3. Pick the right workflow for this task:
   - Creating a new Blueprint or system: use the "build_blueprint_feature" prompt
   - Modifying an existing Blueprint: use the "modify_existing_blueprint" prompt
   - Fixing compile errors: use the "fix_compile_errors" prompt
   - Researching patterns before deciding on an approach: use the "research_reference_patterns" prompt

4. When building, compile after completing each function's graph logic. Fix the first compile error before writing more logic.

5. When done, verify your work compiles cleanly and contains the logic you intended. Use the "verify_and_finish" prompt if available.

Read before write. Compile after changes. Complete the full task.
