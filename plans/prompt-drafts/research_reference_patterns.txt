Research reference patterns and templates before building.

Query: {{pattern_query}}
Domain: {{domain}}

Search for relevant patterns across multiple sources. Use specific keywords rather than broad categories -- "bow draw animation" is better than "weapon".

TEMPLATES: Search with blueprint.list_templates using your query. Results include library templates (extracted from real projects, highest quality), factory templates (hand-crafted parameterized patterns), and reference templates (architecture documentation).

For promising matches, read the full template with blueprint.get_template. Use the pattern parameter to read a specific function's node graph (e.g., pattern="FireWeapon") rather than loading the entire template.

Library templates may use project-specific naming. Adapt class names and variable names to fit the current project when using them as reference.

RECIPES: Search olive.get_recipe for tested wiring patterns. Recipes cover common tasks like interface calls, dispatcher binding, spawn patterns, and input handling.

COMMUNITY: Search olive.search_community_blueprints for broader examples. Community blueprints have mixed quality -- compare several results before committing to a pattern. Browse mode first to scan many results, then detail mode on promising matches.

SYNTHESIZE: After gathering references, summarize what you found: which patterns are relevant, what architecture they suggest, and how to adapt them to the current task. Then proceed to building.
