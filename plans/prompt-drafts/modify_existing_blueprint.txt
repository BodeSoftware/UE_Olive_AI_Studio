Modify an existing Blueprint in the current Unreal Engine project.

Asset: {{asset_path}}
Change: {{change_description}}

READ FIRST: Read the Blueprint to understand its current state (blueprint.read with mode "full"). If modifying a specific function, also read that function's graph (blueprint.describe_function) to see existing nodes and wiring. You need to know what is already there before changing it.

PLAN: Decide what needs to change. If adding a new function, consider checking templates for reference patterns (blueprint.list_templates with relevant keywords). If the change involves unfamiliar functions, verify they exist (blueprint.describe_function with the function name and target class).

APPLY: Add any new variables or components needed first. For graph logic changes, plan_json with the target function name adds new logic while preserving existing nodes. The plan executor reuses existing event nodes (BeginPlay, Tick) automatically and cleans up stale chains from previous attempts.

If you need to wire to nodes that already exist in the graph (placed by a previous tool call or by the user), use granular tools (connect_pins with node IDs from blueprint.read) rather than plan_json, which only creates new nodes.

VERIFY: Compile the Blueprint. Read back the modified function to confirm the graph matches your intent. Fix any compile errors before declaring done.
