TAGS: fix wiring error connection pin connect_pins set_pin_default
---
FIX wiring after partial plan success:
1. Read the apply_plan_json result -- it contains wiring_errors and pin_manifests
2. Each failed connection lists ACTUAL available pins: {name, type, direction}
3. Use blueprint.connect_pins with exact pin names from the manifest

Pin manifests are ground truth -- use those exact names, do not guess.
Node IDs come from step_to_node_map in the apply result -- never invent them.
Common mismatch: AI uses "Location" but SpawnActor has "SpawnTransform" (Transform).
If multiple connections failed, batch all connect_pins calls in one turn.
If fixing a function graph, pass the function name as graph parameter.
