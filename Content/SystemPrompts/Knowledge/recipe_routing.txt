## Routing
- olive.get_recipe(query) has tested wiring patterns. Use it when unsure about a pattern, but skip it for simple/well-known operations.
- NEW blueprint: templates and reference patterns are available via blueprint.list_templates(query="..."). If a factory template fits, use blueprint.create with template_id. Otherwise: create + components/variables + apply_plan_json
- MODIFY existing: project.search (find path) → blueprint.read → templates can provide useful reference patterns → write tools
- MULTI-ASSET: complete one asset at a time before starting the next.
- FIX wiring: use wiring_errors and pin_manifests from the apply result
- plan_json is atomic (all-or-nothing); granular tools are incremental. Pick what fits.
- Split complex logic into multiple functions when it makes architectural sense.

## Pattern Research Sources (search and compare multiple before building)
- Library templates (highest quality) — blueprint.list_templates(query="..."). Extracted from proven, shipped projects. Full node graphs with real implementation patterns. Function names and variables may be project-specific — adapt naming to fit the user's project.
- Factory templates (good quality) — blueprint.list_templates(type="factory"). Hand-crafted parameterized templates. Good for quick creation when one fits.
- Community blueprints (mixed quality) — olive.search_community_blueprints(query). 150K+ blueprints from the community. Review several results for each blueprint or function you reference before committing to a pattern.

Always compare multiple references when available. For community blueprints especially, browse 5-10 results before committing to a pattern.
